PROJECT_HOME=$1

mkdir $PROJECT_HOME/data
mkdir $PROJECT_HOME/data/raw
mkdir $PROJECT_HOME/data/processed

mkdir $PROJECT_HOME/out
mkdir $PROJECT_HOME/out/binaries
mkdir $PROJECT_HOME/out/plots
mkdir $PROJECT_HOME/out/results
mkdir $PROJECT_HOME/out/scripts

mkdir $PROJECT_HOME/src
mkdir $PROJECT_HOME/src/utils
mkdir $PROJECT_HOME/src/preprocessing
mkdir $PROJECT_HOME/src/analysis
mkdir $PROJECT_HOME/src/training
mkdir $PROJECT_HOME/src/calibration
mkdir $PROJECT_HOME/src/distillation
mkdir $PROJECT_HOME/src/evaluation
